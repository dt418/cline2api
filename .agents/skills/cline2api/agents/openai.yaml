interface:
  display_name: "Cline2api"
  short_description: "Repo-specific patterns and workflows for cline2api"
  default_prompt: "Use the cline2api repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true